spring:
  application:
    name: resource-sample
  cloud:
    config:
      fail-fast: true
      uri: http://config.kimloong.me:8888

security:
  oauth2:
    client:
      clientId: resource-sample
      client-secret: secret123
    resource:
      id: resource-sample