spring:
  application:
    name: uaa
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: config
eureka:
  client:
    service-url:
      defaultZone: http://registry.kimloong.me:8761/eureka/

server:
  port: 5000
  context-path: /uc
logging:
  level:
    org.springframework: debug